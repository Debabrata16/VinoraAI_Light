import React from "react";
import About from "../components/About/About";

// About page — showcases the full About section as a standalone page.
// You can add more sections here (team, mission, values, etc.) as the site grows.
const AboutPage = () => {
  return (
    <>
      <About />
    </>
  );
};

export default AboutPage;
